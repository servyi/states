[package]
name = "servyi-states"
version = "0.1.0"
edition = "2021"
description = "Hierarchical context IDs, phase trackers, and generic state machine infrastructure"
license = "MIT"

[dependencies]
dashmap = "6"
anyhow = "1"
async-trait = "0.1"
futures = "0.3"
